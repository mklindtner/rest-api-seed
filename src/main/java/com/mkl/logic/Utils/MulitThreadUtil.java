package com.mkl.logic.Utils;

import java.util.ArrayList;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;
import java.util.concurrent.Future;
import java.util.List;

public class MulitThreadUtil<Edto>
{
	/*
		note: create interface util extends Com,
		implement Function in multi?


	public Edto threadProblem(int amount) {
		ExecutorService executor = Executors.newFixedThreadPool(10);
		List<Future<Edto>>  futures = new ArrayList<>();
		for(int i = 0; i < amount; i++) {
			Future<Edto> future = executor.submit();
		}
	} */
}
